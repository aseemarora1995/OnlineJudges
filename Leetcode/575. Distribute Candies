class Solution {
public:
    int distributeCandies(vector<int>& candies) {
        
        set<int> s1(candies.begin(), candies.end());
        if(candies.size()/2 >= s1.size())
            return s1.size();
        return candies.size()/2;
    }
};
